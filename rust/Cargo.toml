[package]
name = "csonpath"
version = "0.16.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[build-dependencies]
cc = "1.0"

[dev-dependencies]
serde_json = "1.0"
